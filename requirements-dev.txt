# Optional browser regression tests; the game itself has no Python dependency.
playwright==1.58.0
Pillow==12.3.0
# Only required to regenerate WOFF2 after editing public/font source fonts.
fonttools[woff]==4.65.0
