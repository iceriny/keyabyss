# 项目核心规则

## 平台与布局

- **不需要兼容小屏（移动端）布局。** 游戏面向桌面键盘与鼠标操作，以 1920×1080 为主要视觉基准。
- 除非用户明确要求，不新增或维护移动端专用布局、响应式断点、触屏交互及小屏适配测试；不为兼容手机或平板竖屏而压缩桌面布局、字号或视觉效果。
- 必须支持常见桌面分辨率的全屏与非全屏窗口；布局依据实际 CSS 视口宽高，而非只依据显示器分辨率。以 1024×640 至 3840×2160（含 3440×1440 超宽屏）作为当前验证范围；浏览器工具栏和系统缩放导致的可用空间减少也应保持文字不重叠、操作不出界。此要求与移动端适配分开。
- 历史文档、现有样式和测试中的移动端兼容记录不构成后续适配要求。涉及布局的开发、设计和验收均遵循本规则。
- 用户当前明确指令优先于本文件及历史约定。

相关约定见 [README.md](README.md)、[视觉规则](docs/ART-DIRECTION.md)、[文案与页面规则](docs/CONTENT-STYLE.md)和[验证范围](docs/TESTING.md)。

## 统一 UI 风格

- 所有游戏界面遵循 [开放式 UI 约定](docs/OPEN-UI.md)：场景承载氛围，文字、留白与局部细刻线组织信息；不使用实体面板底板、四边框和圆角卡片。胜负结算和战斗按钮同样遵守。
- 可切换记录的窗口占位必须稳定，长构筑与详情在内部滚动。遗物选择底部的法书、血盾与构筑摘要不得抢占三个选项的视觉中心。
- 弹层外容器、标题、返回与保存等主要操作保持固定；只有正文或列表的内容器允许滚动，禁止整窗滚动带走标题和操作。无需滚动的短页面不强制增加滚动。
- 游戏界面禁止使用 emoji 图标；每件遗物必须有对应且可辨认的 SVG 图案，图鉴与选择页共用。新增遗物需同步补充矢量图，不得退回字体符号占位。
- 生命和护盾均显示进度条；动态连笔与施法反馈不导致 HUD 重排，尊重减少动态设置。

## 字体与启动

- 全局默认使用「汇文明朝体」，小字、辅助说明和词令提示使用「京華老宋体v3.0」；Canvas 战场文字遵循同一规则。
- 两套字体必须随发行包提供，全部加载并解析成功后才允许挂载游戏，包括首屏载入页。此前仅显示使用系统字体的独立轻量等待组件；失败可重试，不以回退字体提前显示游戏。
- 字体使用保留完整字形的 WOFF2，源 TTF/OTF 保留在 `public/font`，发行包只包含压缩版。源字体变化后执行 `npm run fonts:build`。

- 首次访问必须停留在字体准备页，字体就绪后由用户点击「继续进入」才展开游戏；全屏按钮不触发继续。成功继续后才记录已完成，后续访问可在字体就绪后自动进入。
- 准备页使用系统字体与简化开放式 UI，启动 JS/CSS 独立打包并带内容哈希；不依赖 React、Three.js 或游戏主样式。主程序只在通过字体与首次确认两道门槛后加载，同时保留离线 `file://` 支持。
